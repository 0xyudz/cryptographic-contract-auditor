{
  "name": "@pharos/cryptographic-contract-auditor",
  "version": "1.0.0",
  "description": "A deterministic, heuristic-based smart contract security scanner that returns a cryptographically attested risk score.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "SKILL.md",
    "README.md",
    "references"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "cli": "tsx src/interface/cli.ts",
    "prepare": "npm run build"
  },
  "dependencies": {
    "zod": "^3.23.8",
    "viem": "^2.0.0",
    "@langchain/core": "^0.1.0",
    "langchain": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "tsx": "^4.10.1"
  }
}
